enum AppFlavor { dev, qa, production }

enum GenericDialogType {
  pageDialog,
  pageOverlayDialog,
  bottomSheetDialog,
  modalDialog,
}

enum GenericDialogActivityType {
  globalProgressIndicator,
  successDialog,
  logout,
  widgetDialog,
  searchableListDialog
}

enum ResponseMessageType { failure, success, warning }

enum ProjectTheme {
  lightTheme,
  darkTheme,
  systemDefaultTheme,
}

enum DashboardPages { explore, portfolio, earn, spend, connect }
